# Magic SM config

dist: 0.0                   # Not used but should be true input
jump_angle: 30              # Angle of the jump
landing_speed: 30           # Speed at which to go upon landing
landing_time: 0.5           # Time to drive on landing
imu_thresh: 5.0             # Acceleration threshold to determine if landing or taking off
rampup_time: 2.0

# For hardcoding purposes only
drive: 80                   # Takeoff speed

sm:
  update_hz: 50
